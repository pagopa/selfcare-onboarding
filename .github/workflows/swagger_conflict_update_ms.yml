name: Swagger Detect Conflict and Update Onboarding MS
on:
  pull_request:
    branches:
      - main
      - releases/*
      - ops/SELC-3954
    types: [ opened, synchronize, reopened ]
    paths:
      - "apps/onboarding-ms/**"
    
permissions:
  contents: write

jobs:
  swagger_conflict_update:  
    runs-on: ubuntu-latest
    name: Swagger Detect Conflict and Update
    steps:
      - id: swagger-conflict-update
        uses: pagopa/selfcare-commons/github-actions-template/swagger-detect-conflict-quarkus@93eb5ff9c17851fe49ce8d813e35673755e70545
        with:
          path_openapi_docs: src/main/docs
          branch_ref: main
          module: onboarding-ms